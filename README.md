# Time Duration Calculator

A lightweight web application to calculate the exact duration between two times.  
It supports 12-hour and 24-hour inputs, handles across-midnight calculations, and includes an optional date-aware mode for full date-time duration comparisons.
